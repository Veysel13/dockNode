import { createUser, updateUser, deleteUser } from "./userMutation";
import { createPost, updatePost, deletePost } from "./postMutation";
import { createComment, updateComment, deleteComment } from "./commentMutation";

export { createUser, updateUser, deleteUser, createPost, updatePost, deletePost, createComment, updateComment, deleteComment };
